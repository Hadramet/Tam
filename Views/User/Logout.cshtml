@model Tam.webapp.Models.User
@{
    ViewData["Title"] = "Logout";
}

<h1>Do you want to logout?</h1>

<div class="row">
    <div class="col-md-4">
        @*<a asp-action="Stay" class="btn btn-primary btn-group-vertical">Stay Connected</a>*@
        <a asp-action="LogoutAction" class="btn btn-primary btn-group-vertical">Logout</a>
    </div>
</div>
@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}

